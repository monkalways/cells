// import { createSelector } from 'reselect';

// Basic selectors
const isAuthenticated = (state) => state.authentication.authenticated;

export default {
  // Basic selectors
  isAuthenticated,
  // Reselect
};
